This example demonstrates the use of the lwIP network stack via the VFS lwip
plugin. Two POSIX components exchange UDP datagrams. Each component hosts its
own IP stack by instantiating the VFS lwip plugin. The components are
interconnected via a NIC router.
